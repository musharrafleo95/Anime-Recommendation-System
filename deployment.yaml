apiVersion: apps/v1
kind: Deployment
metadata:
  name: anime-recommendation-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: anime-recommendation-app
  template:
    metadata:
      labels:
        app: anime-recommendation-app
    spec:
      containers:
      - name: anime-recommendation-app-container
        image: gcr.io/mlops-471718/ml-project:latest
        ports:
        - containerPort: 5000  # Replace with the port your app listens on
---
apiVersion: v1
kind: Service
metadata:
  name: anime-recommendation-app-service
spec:
  type: LoadBalancer
  selector:
    app: anime-recommendation-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000